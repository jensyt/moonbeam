[package]
name = "examples-concurrent"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
moonbeam = { path = "../../moonbeam", default-features = false, features = ["mt", "macros"] }
