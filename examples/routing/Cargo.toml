[package]
name = "examples-routing"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
moonbeam = { path = "../../moonbeam", default-features = false, features = ["router"] }
moonbeam-serde = { path = "../../moonbeam-serde" }
serde = { version = "1.0", features = ["derive"] }
