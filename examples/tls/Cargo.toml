[package]
name = "examples-tls"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
moonbeam = { path = "../../moonbeam", default-features = false, features = ["macros", "tls", "mt"] }
rcgen = "0.14"
rustls = "0.23"
