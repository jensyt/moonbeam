[package]
name = "moonbeam-serde"
version = "0.1.0"
edition = "2024"
authors = ["Jens Taylor"]
description = "Serde support for Moonbeam"
license = "MIT"

[dependencies]
moonbeam = { path = "../moonbeam" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
